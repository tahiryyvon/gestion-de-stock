{
  "name": "gestion-stock",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run install-all && npx prisma@5.22.0 generate && cd app && npm run build --workspace=packages/web",
    "dev": "cd app && npm run dev --workspace=packages/web",
    "start": "cd app && npm run start --workspace=packages/web",
    "install-all": "cd app && npm install"
  },
  "dependencies": {
    "next": "^14.0.0",
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}